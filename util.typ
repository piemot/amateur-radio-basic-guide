#let github = image("github.svg")
#let maple = image("maple_leaf.svg")

#let icon(
  content,
  size: 0.8em,
  baseline: 0.125em,
) = box(
  content,
  width: size,
  height: size,
  baseline: baseline,
)
